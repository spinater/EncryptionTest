name: GitlabSync

on:
  - push
  - delete

jobs:
  sync:
    runs-on: ubuntu-latest
    name: Git Repo Sync
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: wangchucheng/git-repo-sync@v0.1.0
      with:
        # repo url
        target-url: https://gitlab.com/spinater/sync-from-github.git
        # repo username
        target-username: token_sync
          # git lab token
        target-token: zns8asmTXBJb9xdzjfGY
        #token : glpat-bULMiWf29dyMESyDcsze

        #deploy token : 43xGoif6NMDtzUGeUwYA