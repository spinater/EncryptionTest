name: GitlabSync

on:
  - push
  - delete

jobs:
  sync:
    runs-on: ubuntu-latest
    name: Git Repo Sync
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: wangchucheng/git-repo-sync@v0.1.0
      with:
        # repo url
        target-url: https://gitlab.com/spinater/sync-from-github.git
        # repo username
        target-username: gitlab+deploy-token-1204570
          # git lab token
        target-token: 43xGoif6NMDtzUGeUwYA